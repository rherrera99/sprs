import { ViewContainerRef } from '@angular/core';
export declare class MdTransclude {
    viewRef: ViewContainerRef;
    private _mdTransclude;
    constructor(viewRef: ViewContainerRef);
    private mdTransclude;
}
